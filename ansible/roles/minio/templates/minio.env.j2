# {{ ansible_managed }}
MINIO_ROOT_USER="{{ minio_root_user }}"
MINIO_ROOT_PASSWORD="{{ minio_root_password }}"
MINIO_ADDRESS="{{ minio_listen_address }}"
MINIO_CONSOLE_ADDRESS=":{{ minio_console_port }}"
MINIO_REGION="{{ minio_region }}"
